FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy MCP server and tools
COPY mcp_server.py .
COPY ../tools /app/tools

# Expose MCP port
EXPOSE 8081

# Run the MCP server
CMD ["python", "mcp_server.py"]
